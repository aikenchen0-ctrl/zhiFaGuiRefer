# Android 操作技能契约

技能必须包含：标识、版本、目标应用、参数、前置条件、定位器、动作步骤、中间检查点、成功判据、失效判据、超时和回退策略。

## 回退规则

1. 定位器失败：重新获取 UI 树并尝试替代定位器。
2. 页面状态不匹配：停止当前技能，交给 GUI Agent 重新规划。
3. 参数或权限失败：不重试危险动作，进入人工确认。
4. 超时或应用异常：恢复最近检查点，超过次数后标记技能版本失效。

未经真实设备回放验证的候选技能只能进入候选区，不得进入可执行技能库。

